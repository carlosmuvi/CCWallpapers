<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/layout_home_options_bottom_menu_container"
    android:layout_width="match_parent"
    android:layout_height="300dp"
    android:background="#FFFFFF"
    android:orientation="vertical"
    app:behavior_hideable="true"
    app:behavior_peekHeight="64dp"
    app:layout_behavior="@string/bottom_sheet_behavior"
    >

  <TextView
    android:id="@+id/layout_home_options_bottom_menu_text_download"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:text="Texto de prueba"
    android:textAppearance="@style/TextAppearance.AppCompat.Body1"
    />

  <TextView
      android:id="@+id/layout_home_options_bottom_menu_text_setaswpp"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:padding="16dp"
      android:text="Texto de prueba"
      android:textAppearance="@style/TextAppearance.AppCompat.Body1"
      />

  <TextView
      android:id="@+id/layout_home_options_bottom_menu_text_share"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:padding="16dp"
      android:text="Texto de prueba"
      android:textAppearance="@style/TextAppearance.AppCompat.Body1"
      />

</LinearLayout>