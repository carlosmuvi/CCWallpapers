<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/item_home_wallpaper_height"
    android:orientation="vertical"
    >

  <ImageView
      android:id="@+id/home_item_imageview"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      tools:scaleType="centerCrop"
      tools:src="@drawable/tools_sampleimage"
      />

  <RelativeLayout
      android:layout_width="match_parent"
      android:layout_height="90dp"
      android:layout_gravity="bottom"
      android:background="@drawable/item_home_wallpaper_actionsframe_color"
      android:padding="16dp"
      >

    <ImageView
        android:id="@+id/home_item_options_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_options_24dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        />

    <TextView
        android:id="@+id/home_item_title"
        android:layout_width="wrap_content"
        android:layout_height="30dp"
        android:gravity="center"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:layout_marginRight="12dp"
        android:layout_toLeftOf="@id/home_item_options_button"
        android:layout_toStartOf="@id/home_item_options_button"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        tools:text="San Francisco, CA, Estados Unidos de Norte América"
        />
  </RelativeLayout>

</FrameLayout>